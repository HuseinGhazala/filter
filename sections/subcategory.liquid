{%- assign color_collection_handles = 'black,gray,indigo-blue,lavender,navy-blue,olive-green,sky-blue' | split: ',' -%}
{%- assign is_color_collection = false -%}
{%- if collection != blank and color_collection_handles contains collection.handle -%}
  {%- assign is_color_collection = true -%}
{%- endif -%}
{%- assign is_all_scrubs_collection = false -%}
{%- if collection != blank -%}
  {%- if collection.handle == 'all-scrubs' or collection.handle == 'all' -%}
    {%- assign is_all_scrubs_collection = true -%}
  {%- endif -%}
{%- endif -%}

{%- unless is_color_collection -%}
<div class="color-collections-section page-width wt-slider__container--featured" id="section-{{ section.id }}">
  <div class="section-header text-center">
    {% if section.settings.title != blank %}
      <h2 class="color-collections-title">{{ section.settings.title | escape }}</h2>
    {% endif %}
    {% if section.settings.sub_title != blank %}
      <p class="color-collections-subtitle">{{ section.settings.sub_title | escape }}</p>
    {% endif %}
  </div>

  {%- if is_all_scrubs_collection -%}
  <div class="swiper color-collections-swiper-{{ section.id }}">
    <div class="swiper-wrapper">
      {%- assign current_collection_handle = collection.handle -%}
      {% if section.settings.collection != blank %}
        {% for sub_collection in section.settings.collection %}
          {%- if sub_collection.handle == current_collection_handle -%}
            {%- continue -%}
          {%- endif -%}

          {%- capture filter_param -%}
            ?filter.v.option.color={{ sub_collection.title | url_encode }}
          {%- endcapture -%}

          <div class="swiper-slide">
            <a href="{{ sub_collection.url }}{{ filter_param }}" class="color-collection-card">
              <div class="card-image-wrapper">
                {% if sub_collection.image != blank %}
                  {{
                    sub_collection.image
                    | image_url: width: 1200
                    | image_tag:
                      loading: 'lazy',
                      widths: '400, 600, 800, 1000, 1200',
                      sizes: '(min-width: 990px) 34vw, 100vw',
                      alt: sub_collection.title
                  }}
                {% elsif sub_collection.products.first.featured_image != blank %}
                  {{
                    sub_collection.products.first.featured_image
                    | image_url: width: 1200
                    | image_tag:
                      loading: 'lazy',
                      widths: '400, 600, 800, 1000, 1200',
                      sizes: '(min-width: 990px) 34vw, 100vw',
                      alt: sub_collection.products.first.featured_image.alt
                  }}
                {% else %}
                  {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                {% endif %}
              </div>
              <h3 class="card-title">{{ sub_collection.title }}</h3>
            </a>
          </div>
        {% endfor %}
      {% else %}
        {% for i in (1..6) %}
          <div class="swiper-slide">
            <div class="color-collection-card dummy-card">
              <div class="card-image-wrapper">
                {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
              </div>
              <h3 class="card-title">اسم الكوليكشن</h3>
            </div>
          </div>
        {% endfor %}
      {% endif %}
    </div>
  </div>
  {%- endif -%}
</div>
{%- endunless -%}

{% style %}
  #section-{{ section.id }} {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }

  /* Separate Text Colors */
  #section-{{ section.id }} .color-collections-title {
    color: {{ section.settings.title_color }};
  }

  #section-{{ section.id }} .color-collections-subtitle {
    color: {{ section.settings.subtitle_color }};
  }

  #section-{{ section.id }} .color-collection-card .card-title {
    color: {{ section.settings.card_title_color }};
  }

  /* Section Header */
  .section-header.text-center {
    text-align: center;
    margin-bottom: 32px;
  }
  .color-collections-section{
    width:60%;
  
  }

  .color-collections-title {
    font-size: 30px;
    font-weight: 700;
    margin: 0 0 8px 0;
  }

  .color-collections-subtitle {
    font-size: 16px;
    margin: 0 auto;
    max-width: 650px;
    line-height: 1.6;
  }

  /* Swiper Container Fixes */
  .color-collections-swiper-{{ section.id }} {
    padding-bottom: 10px;
    overflow: hidden;
    width: 100%;
  }

  /* قبل ما Swiper يتحمل ويحسب عرض السلايدز، نديله عرض افتراضي مطابق (4 أعمدة)
     عشان مايظهرش كارد بالعرض الكامل لحظة قبل ما JS يشتغل */
  .color-collections-swiper-{{ section.id }} .swiper-wrapper {
    display: flex;
  }

  .color-collections-swiper-{{ section.id }} .swiper-slide {
    flex-shrink: 0;
    width: calc(25% - 12px);
  }

  .color-collection-card {
    display: block;
    text-decoration: none;
    text-align: center;
  }

  /* Square Image Wrapper */
  .card-image-wrapper {
    position: relative;
    width: 100%;
    aspect-ratio: 1 / 1;
    border-radius: 12px;
    overflow: hidden;
  }
  
  .collection-banner .card-image-wrapper{
    aspect-ratio: 1 / 1;
    width: 175px !important;
    height: 175px !important;
  }
  .card-image-wrapper img,
  .card-image-wrapper svg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: contain;
    transition: transform 0.4s ease;
  }

  .color-collection-card:hover .card-image-wrapper img {
    transform: scale(1.05);
  }

  .color-collection-card .card-title {
    font-size: 18px;
    margin-top: 12px;
    margin-bottom: 0;
    font-weight: 600;
    text-align: center;
  }

  /* Mobile Responsive Controls */
  @media screen and (max-width: 749px) {
     .color-collections-section{
    width:100%;
  }

    {% if section.settings.show_mobile_spacing %}
      #section-{{ section.id }} {
        padding-top: {{ section.settings.margin_top_mobile }}px;
        padding-bottom: {{ section.settings.margin_bottom_mobile }}px;
      }
    {% endif %}

    .color-collections-title {
      font-size: 22px;
    }

    .color-collections-subtitle {
      font-size: 14px;
    }

    .color-collection-card .card-title {
      font-size: 15px;
    }
  }
{% endstyle %}

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const swiperContainer = document.querySelector('.color-collections-swiper-{{ section.id }}');

    if (swiperContainer) {
      new Swiper(swiperContainer, {
        slidesPerView: 4,
        spaceBetween: 16,
        grabCursor: true,
        // هذه الإعدادات تمنع المشاكل عند تغيير حجم الشاشة (Responsive Glitches)
        observer: true,
        observeParents: true,
        resizeObserver: true,
        breakpoints: {
          480: {
            slidesPerView: 4,
            spaceBetween: 16,
          },
          768: {
            slidesPerView: 4,
            spaceBetween: 20,
          },
          1024: {
            slidesPerView: 4,
            spaceBetween: 24,
          },
        },
      });
    }
  });
</script>

{% schema %}
{
  "name": "Sub Category Collections",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Main Heading",
      "default": "كل السكرابات"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Main Heading Color",
      "default": "#11173D"
    },
    {
      "type": "text",
      "id": "sub_title",
      "label": "Sub Heading",
      "default": "سكرابات متنوعة بكل الألوان و القصات و الستايلات التي تحتاجينها في يومك"
    },
    {
      "type": "color",
      "id": "subtitle_color",
      "label": "Sub Heading Color",
      "default": "#11173D"
    },
    {
      "type": "color",
      "id": "card_title_color",
      "label": "Cards Title Color",
      "default": "#11173D"
    },
    {
      "type": "header",
      "content": "Section Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 36
    },
    {
      "type": "header",
      "content": "Mobile Spacing"
    },
    {
      "type": "checkbox",
      "id": "show_mobile_spacing",
      "label": "Enable mobile custom spacing",
      "default": true
    },
    {
      "type": "range",
      "id": "margin_top_mobile",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 20,
      "unit": "px",
      "label": "Top padding (Mobile)"
    },
    {
      "type": "range",
      "id": "margin_bottom_mobile",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 20,
      "unit": "px",
      "label": "Bottom padding (Mobile)"
    },
    {
      "type": "collection_list",
      "id": "collection",
      "limit": 10,
      "label": "Collections"
    }
  ],
  "presets": [
    {
      "name": " Sub Category Collections"
    }
  ]
}
{% endschema %}